jochre {
  search {
    locale = "yi"

    highlighter {
      formatter-pre-tag = "<b>"
      formatter-post-tag = "</b>"
    }

    database {
      url = "jdbc:postgresql://localhost:5433/jochre_search_test"
      url = ${?POSTGRES_TEST_URL}
    }
  }
}